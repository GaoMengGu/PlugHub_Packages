<Window x:Class="PlugHub.ClearHeightAnalysis.UI.AnalysisResultWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="净高分析结果"
        Width="980" Height="560"
        MinWidth="820" MinHeight="480"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Microsoft YaHei"
        FontSize="13">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="16" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="净高分析结果" FontSize="18" FontWeight="SemiBold" Foreground="#2B579A"/>
                <TextBlock x:Name="SummaryText" Foreground="#666666" Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="12">
            <DataGrid x:Name="ResultsGrid"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      AlternatingRowBackground="#FAFAFA">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="楼层" Binding="{Binding LevelName}" Width="80"/>
                    <DataGridTextColumn Header="网格编号" Binding="{Binding GridNumber}" Width="90"/>
                    <DataGridTextColumn Header="中心 X" Binding="{Binding CenterX}" Width="90"/>
                    <DataGridTextColumn Header="中心 Y" Binding="{Binding CenterY}" Width="90"/>
                    <DataGridTextColumn Header="净高" Binding="{Binding ClearHeight}" Width="80"/>
                    <DataGridTextColumn Header="阈值" Binding="{Binding Threshold}" Width="80"/>
                    <DataGridTextColumn Header="差值" Binding="{Binding Difference}" Width="80"/>
                    <DataGridTextColumn Header="风险" Binding="{Binding RiskLevel}" Width="80"/>
                    <DataGridTextColumn Header="控制构件" Binding="{Binding ControllingElement}" Width="160"/>
                    <DataGridTextColumn Header="类别" Binding="{Binding Category}" Width="110"/>
                    <DataGridTextColumn Header="来源模型" Binding="{Binding SourceModel}" Width="140"/>
                    <DataGridTextColumn Header="ElementId" Binding="{Binding ElementKey}" Width="100"/>
                    <DataGridCheckBoxColumn Header="链接" Binding="{Binding IsFromLink}" Width="60"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Content="关闭" Padding="20,8" Background="#2B579A" Foreground="White" BorderThickness="0" Click="CloseButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
