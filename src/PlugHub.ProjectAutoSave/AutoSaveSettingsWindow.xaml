<Window x:Class="PlugHub.ProjectAutoSave.AutoSaveSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="自动保存设置"
        Width="520" Height="420"
        MinWidth="460" MinHeight="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Microsoft YaHei"
        FontSize="13"
        ResizeMode="NoResize">

    <Window.Resources>
        <Style x:Key="FieldTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6,8,6"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2B579A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8,20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3A6DB5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E3F73"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="20,8,20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}"
                                CornerRadius="4" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B0B0B0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20,20,20,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="6" Padding="16" Margin="0,0,0,14">
            <StackPanel>
                <TextBlock Text="自动保存设置" FontSize="18" FontWeight="SemiBold" Foreground="#2B579A"/>
                <TextBlock Text="启用后将在本次 Revit 会话内，按指定分钟间隔保存当前已保存过的 Revit 项目文件。"
                           Foreground="#666666" Margin="0,6,0,0" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Background="White" CornerRadius="6" Padding="16" Margin="0,0,0,12">
            <StackPanel>
                <CheckBox x:Name="EnableAutoSaveCheckBox"
                          Content="启用自定义保存"
                          FontWeight="SemiBold"
                          Foreground="#333333"/>

                <Grid Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="110"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="分钟间隔" VerticalAlignment="Center" Foreground="#333333"/>
                    <TextBox Grid.Column="1" x:Name="IntervalTextBox" Style="{StaticResource FieldTextBox}"/>
                    <TextBlock Grid.Column="2" Text="范围 1-240 分钟" VerticalAlignment="Center" Foreground="#888888" Margin="10,0,0,0"/>
                </Grid>

                <CheckBox x:Name="ShowNotificationCheckBox"
                          Content="自动保存后弹窗提示"
                          Margin="0,14,0,0"
                          Foreground="#333333"/>
            </StackPanel>
        </Border>

        <TextBlock Grid.Row="2" x:Name="ValidationText"
                   Foreground="#A33" FontSize="12" Margin="4,0,0,0"/>

        <TextBlock Grid.Row="3"
                   Text="提示：重启 Revit 后需再次打开本功能以启动监听；未保存到磁盘的新项目、只读项目、族文档或正在事务中的项目会自动跳过。"
                   Foreground="#888888" FontSize="12" TextWrapping="Wrap" VerticalAlignment="Bottom"/>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,18,0,0">
            <Button Content="取消" Style="{StaticResource SecondaryButton}" Click="CancelButton_Click" Margin="0,0,10,0"/>
            <Button Content="保存设置" Style="{StaticResource PrimaryButton}" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
